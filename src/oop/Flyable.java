package oop;

// What is interfaces?
// It is "simple" class that:
// does not have constructors
// does not have variables
// can have a methods without implementation ( body)

public interface Flyable {
    public void fly(); // add a method
}
